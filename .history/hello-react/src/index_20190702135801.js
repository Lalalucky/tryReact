// 1.React 和 ReactDOM 必须这么写
import React from "react";
import ReactDom from "react-dom";

// 2.创建虚拟DOM元素
const myh1 = React.createElement("h1", null, "这是一个大大的h1标签");

// 3.使用ReactDom把虚拟DOM渲染到页面上
ReactDom.render(my)
